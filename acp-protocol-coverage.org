#+title: ACP Protocol Coverage (cursor-acp)
#+author: Cursor ACP audit
#+date: 2026-04-30

* Scope
This report compares [[https://agentclientprotocol.com/protocol/overview][ACP protocol overview]] against the current implementation in the split Cursor ACP modules.

- Implementation analyzed: =cursor-acp-core.el=, =cursor-acp-ui.el=, =cursor-acp-transport.el=, =cursor-acp-commands.el= (via =cursor-acp.el= loader)
- Status labels: =Done=, =Partial=, =Not Implemented=

* Coverage Table
| Protocol Area            | Feature / Method                               | Status                   | Current Implementation                                                                                                   | Remaining Scope                                                                                                               |
|--------------------------+------------------------------------------------+--------------------------+--------------------------------------------------------------------------------------------------------------------------+-------------------------------------------------------------------------------------------------------------------------------|
| Communication Model      | JSON-RPC 2.0 request/response + notifications  | Done                     | Outbound requests use =jsonrpc=2.0 with ids; notifications handled in =cursor-acp--handle-message= for =session/update=. | None for baseline transport.                                                                                                  |
| Initialization Phase     | =initialize=                                   | Done                     | Implemented in =cursor-acp--handshake= via =cursor-acp--rpc-call "initialize"=.                                          | Optional: validate negotiated protocol version explicitly.                                                                    |
| Initialization Phase     | =authenticate=                                 | Done                     | Implemented in =cursor-acp--handshake= with =authenticate= and =methodId "cursor_login"=.                                | Optional: support alternate auth methods / auth failure UX.                                                                   |
| Session Setup            | =session/new=                                  | Done                     | Implemented in =cursor-acp--handshake=; stores =sessionId= and caches options/capabilities.                              | Optional: richer session metadata persistence.                                                                                |
| Session Setup (optional) | =session/list=                                 | Done                     | Implemented in =cursor-acp-switch-session= via =cursor-acp--rpc-session-list= (capability gated).                        | Add pager support (=nextCursor=), and better UI for large lists (search/filter/sort).                                          |
| Session Setup (optional) | =session/load=                                 | Partial                  | Implemented in =cursor-acp-switch-session= via =cursor-acp--rpc-session-load= (capability gated).                        | History replay not fully rendered yet: handle replay update types (e.g. =user_message_chunk=) and any assistant replay variants. |
| Session Setup (optional) | =session/resume=                               | Done                     | Implemented in =cursor-acp-switch-session= via =cursor-acp--rpc-session-resume= (capability gated).                      | If agent omits resume capability, continue falling back to =session/load=.                                                     |
| Session Setup (optional) | =session/close=                                | Not Implemented          | No =session/close= call path exists.                                                                                     | Requires =initialize.agentCapabilities.sessionCapabilities.close=. Use to free agent-side resources for inactive sessions.    |
| Prompt Turn              | =session/prompt=                               | Done                     | Implemented in =cursor-acp-send= and command execution path.                                                             | Optional: structured prompt content types beyond plain text.                                                                  |
| Prompt Turn              | =session/update= handling                      | Partial                  | Handles =agent_message_chunk= text streaming, mode/model/config/commands cache updates.                                  | Add full update taxonomy handling: thought/user chunks, plan updates, tool state lifecycle, richer rendering by content type. |
| Prompt Turn              | turn end on prompt response (=stopReason=)     | Done                     | Detects terminal stop reasons (including =end_turn= and cancellation variants) and finalizes assistant block/spinner.    | Optional: add explicit UX messaging for non-standard stop reasons (e.g. length/tool limits).                                  |
| Prompt Turn              | cancellation via =session/cancel=              | Done                     | Implemented via =cursor-acp-cancel-turn= (key: =C-c C-x=), sends protocol cancel notification and keeps process alive.   | Optional: add in-flight turn id tracking if future ACP servers require turn-scoped cancellation payloads.                      |
| Agent Optional           | =session/set_mode=                             | Partial                  | Supports mode switch via =session/set_config_option= when advertised; fallback uses =session/set_mode=.                  | Normalize to protocol-preferred path per negotiated capabilities; explicit compatibility matrix.                              |
| Client Baseline Method   | permission request handling (agent->client)    | Done                     | Handles =session/request_permission= request: renders request in chat, prompts in minibuffer, responds with =outcome:selected= + chosen =optionId=; header shows =awaiting-permission=. | Optional: allow/deny “always” persistence policies; richer details view; queue multiple concurrent requests.                  |
| Client Optional Method   | fs.readTextFile                                | Done                     | Implements agent->client =fs/read_text_file= in =cursor-acp--handle-message= via =cursor-acp--fs-read-text-file=; enforces absolute path, workspace-root containment, and supports 1-based =line= + =limit= (line count). Client advertises =clientCapabilities.fs.readTextFile=true=. | Optional: support non-workspace read policy (explicit allowlist), binary files, and richer error UI states.                   |
| Client Optional Method   | fs.writeTextFile                               | Done                     | Implements agent->client =fs/write_text_file= in =cursor-acp--handle-message= via =cursor-acp--fs-write-text-file=; enforces absolute path + workspace-root containment; creates parent dirs; writes file; emits a chat note and records a per-project “seed” for later ediff review. Client advertises =clientCapabilities.fs.writeTextFile=true=. | Optional: atomic writes/backups; configurable write policy; richer error UI states.                                           |
| Client Optional Method   | terminal.create / read / wait / kill / release | Not Implemented          | Client advertises =terminal=false=. No terminal RPC handlers.                                                            | Implement terminal capability surface and lifecycle API handlers, then advertise capability true.                             |
| Notifications/Updates    | advertised commands updates                    | Done                     | Parses command updates from =session/update= and exposes in UI.                                                          | Optional: diff/highlight added/removed commands.                                                                              |
| Notifications/Updates    | mode change updates                            | Done                     | Parses mode change fields in =session/update=.                                                                           | Optional: explicit mode-change event logging in chat stream.                                                                  |
| Notifications/Updates    | plans/tool-call updates                        | Partial                  | Tracks =tool_call= metadata (=toolCallId=, title/kind/status) and on =tool_call_update= completion renders raw output; also detects embedded =diff= content blocks (path/old/new) and renders unified diff previews + saves per-project “seed” files for review. | Add full tool-call lifecycle rendering beyond “completed” (status transitions, incremental updates, richer content rendering). |
| Argument Requirements    | absolute file paths, 1-based lines             | Partial                  | Enforced for filesystem methods: fs requires absolute paths and rejects paths outside workspace; fs line reads accept 1-based =line= and =limit= (line count). | Extend enforcement to any future file/line RPC surfaces (edits/patches), plus consistent error reporting.                     |
| Error Handling           | JSON-RPC error object handling                 | Partial                  | Request path stores =error= and raises Elisp errors; parse failures logged.                                              | Add structured user-facing error states per method/notification class; preserve error codes/messages in UI.                   |
| Extensibility            | custom methods (= _*=), =_meta= fields         | Not Implemented          | No explicit custom method registration/handling.                                                                         | Add generic dispatch for custom methods and pass-through support for =_meta= in requests/responses.                           |

* High-Level Completion
- Core baseline agent-side flow (initialize/auth/new/prompt/update/end-turn): largely implemented.
- Biggest missing protocol surface is transcript replay completeness for multi-session (=session/load=), terminal capability surface, and fuller tool-call lifecycle rendering.
- Practical next milestone: finish =session/load= history rendering (replay update taxonomy), then move to terminal and fuller tool-call lifecycle rendering.

* Suggested Implementation Order
1. Finish =session/load= transcript replay: implement rendering for replay update types (at minimum =user_message_chunk=, plus any assistant chunk variants the agent emits during replay).
2. Improve session picker UX: paging for =session/list= (=nextCursor=), and display more metadata (cwd, updatedAt, tags).
3. Optional: add =session/close= if agent supports it (and a cleanup command).
4. Terminal capability methods.
5. Richer =session/update= support for tool calls and plans (full lifecycle, not only completion rendering).

* UI Notes (current state)
- Chat buffer major mode: =cursor-acp-chat-mode= derived from =markdown-mode= (when installed).
- Keybindings: ACP keys are provided via an emulation keymap so they continue to work even under markdown.
- Permission UX:
  - Requests render in chat with command fenced blocks and a bold Reason section.
  - Minibuffer prompt waits indefinitely.
  - If the prompt is cancelled, the request stays pending and can be retried via =C-c C-a= (=cursor-acp-reprompt-permission=).
- Tool call UX:
  - When =tool_call_update.rawOutput= is present, it is printed in fenced =text= blocks, split per key (e.g. stdout/stderr).
  - If a tool payload contains embedded =diff= blocks (path/old/new), a unified diff preview is rendered and a per-project “seed” file is stored for later =ediff= review.
